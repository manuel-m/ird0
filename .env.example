PROJECT_NAME=insure

# Database credentials
POSTGRES_USER=directory_user
POSTGRES_PASSWORD=change_this_secure_password

# Vault dev token (optional, defaults to dev-root-token)
VAULT_DEV_TOKEN=dev-root-token
