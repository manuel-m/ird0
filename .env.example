# Database credentials - copy to .env and set secure values
POSTGRES_USER=directory_user
POSTGRES_PASSWORD=change_this_secure_password

# Vault configuration
# Set VAULT_ENABLED=false to disable Vault and use environment variables
VAULT_ENABLED=true
VAULT_ADDR=http://vault:8200
VAULT_DEV_TOKEN=dev-root-token
